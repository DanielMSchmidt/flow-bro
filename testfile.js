import React, { PropTypes } from 'react';

EventsScreen.propTypes = {
    ass: PropTypes.shape({
        fo: PropTypes.shape({
            asd: PropTypes.arrayOf(
                PropTypes.shape({
                    ccc: PropTypes.string.isRequired,
                    asd: PropTypes.shape({
                        yxc: PropTypes.string.isRequired,
                    }).isRequired,
                    yxcxcy: PropTypes.number.isRequired,
                    yxcxcy12: PropTypes.boolean.isRequired,
                }),
            ).isRequired,
        }).isRequired,
    }),
    asasas: PropTypes.func.isRequired,
    qwe: PropTypes.func,
};
